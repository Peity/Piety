export enum MemberTypes {
    Warrior = `Warrior`,
    Farmer = `Farmer`
}

export enum MemberState {
    Farming = `Farming`,
    Defending = `Defending`,
    Idle = `Idle`,
    Invading = `Invading`,
    OnMission = `On Mission`
}